import Stack from "@mui/material/Stack";

import { UserForm } from "../components/UserForm";

export const CreateUser = () => {
  return (
    <Stack spacing={2}>
      <UserForm />
    </Stack>
  );
};
